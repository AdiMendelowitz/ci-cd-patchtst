# Core runtime dependencies for the ci-cd-patchtst experiments.
# Tested on Python 3.12. The versions below are minimums, not the exact
# tested set. For a reproducible pin, regenerate from your own environment:
#     uv pip freeze > requirements.txt
# To confirm the list matches the actual imports under src/:
#     pipreqs src/

torch>=2.2
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.8
seaborn>=0.13

# Development and notebook tooling (install separately if needed):
#   jupyter, ipykernel, nbstripout, black, flake8, pytest
